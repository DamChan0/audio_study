[package]
name = "mod_player_examples"
version = "0.1.0"
edition = "2024"

[dependencies]
cpal = "0.15"
anyhow = "1"
crossbeam-channel = "0.5"

# 01의 examples crate를 cpal_examples 라는 이름으로 import
cpal_examples = { path = "../../01_cpal/examples" }